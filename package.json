{
  "name": "lovelace-xiaomi-vacuum-card",
  "version": "4.6.3",
  "private": true,
  "description": "Maintained Xiaomi Vacuum Card Reborn for Home Assistant Lovelace",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run check:build && npm run check:syntax && npm run check:version && npm run lint && npm run format:check && npm test",
    "check:build": "npm run build && git diff --exit-code dist/",
    "check:syntax": "node --check src/lit.js src/constants.js src/styles.js src/utils.js src/card.js src/editor.js src/xiaomi-vacuum-card.js dist/xiaomi-vacuum-card.js",
    "check:version": "node tests/check-version.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:component": "playwright test --config playwright.component.config.mjs",
    "test:ha-smoke": "playwright test tests/ha-smoke",
    "lint": "npm run lint:js && npm run lint:md",
    "lint:js": "eslint .",
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "lit": "^3.3.3",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.6"
  }
}
